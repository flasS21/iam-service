Session Authority Test UI

Purpose:
Test frontend for the IAM service gateway. Validates Kong routing behavior, OAuth/OIDC authentication flow, FQDN-based route isolation, and IP allowlist enforcement.

Usage:
Served via Kong gateway at the root path. Navigate to dashboard for authenticated session testing, or use the homepage to verify public route accessibility and IP restrictions.
